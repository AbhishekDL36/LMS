═══════════════════════════════════════════════════════════════════════════════
FIX: Unknown Teacher & Created N/A - Step by Step
═══════════════════════════════════════════════════════════════════════════════

PROBLEM:
- Admin Courses page shows "Unknown Teacher" 
- Admin Courses page shows "Created: N/A"
- Old courses in database missing teacherId and createdAt

ROOT CAUSE:
- Course schema was missing createdAt/updatedAt fields
- Old courses don't have teacherId set

═══════════════════════════════════════════════════════════════════════════════
QUICK FIX (5 minutes)
═══════════════════════════════════════════════════════════════════════════════

STEP 1: Update Course Schema
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✓ Already done - backend/models/Course.js updated with:
  - createdAt field
  - updatedAt field
  - teacherId marked as required

STEP 2: Clear Old Course Data
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Open MongoDB Atlas (or your MongoDB dashboard)
2. Find your LMS database
3. Go to Collections
4. Click on "courses"
5. Click "Delete all documents"
6. Also delete all documents in "enrollments" collection

STEP 3: Restart Backend
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Stop the backend server (Ctrl+C)
2. Run: npm run dev

STEP 4: Create New Courses
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Login as Teacher
2. Click "Create Course"
3. Fill in Title and Description
4. Click "Create Course"

STEP 5: Verify in Admin Panel
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1. Login as Admin
2. Go to Admin Dashboard → All Courses
3. Check that courses show:
   ✓ Correct teacher name (NOT "Unknown Teacher")
   ✓ Correct created date (NOT "N/A")

═══════════════════════════════════════════════════════════════════════════════
VERIFICATION CHECKLIST
═══════════════════════════════════════════════════════════════════════════════

After completing steps above:

✓ Admin Courses shows teacher name instead of "Unknown Teacher"
✓ Admin Courses shows date instead of "N/A"
✓ Course cards display correct teacher email
✓ Created date is today's date or course creation date
✓ No "Invalid Date" errors in console

═══════════════════════════════════════════════════════════════════════════════
IF STILL BROKEN
═══════════════════════════════════════════════════════════════════════════════

1. Check that Course schema has createdAt field
   File: backend/models/Course.js
   Should have: createdAt: { type: Date, default: Date.now }

2. Check MongoDB - courses collection should be empty
   - All old documents should be deleted

3. Check teacher name in database
   - When creating course, backend sets: teacherId: req.user.id
   - Admin panel should populate teacher name from userId

4. Check backend logs for errors
   - npm run dev should show "Course created successfully"

═══════════════════════════════════════════════════════════════════════════════
